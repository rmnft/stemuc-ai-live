# ==========================================
# DOCKER ENVIRONMENT CONFIGURATION
# ==========================================

# Paths for Docker environment
UPLOAD_DIR=/app/uploads
OUTPUT_DIR=/app/separated
MODELS_DIR=/app/models
LOG_FILE=/app/logs/app.log

# Performance settings for containers
MAX_FILE_SIZE=157286400
USE_GPU=false
WORKERS=1

# Logging
LOG_LEVEL=INFO

# Security
NODE_ENV=production

# Required tokens (set these in Railway/Docker)
HUGGINGFACE_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PYANNOTE_API_KEY=pya_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx 